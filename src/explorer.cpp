#include "explorer.hpp"

#include <iostream>
#include <string>
#include "color.hpp"
void open_explorer(const std::string& username, const std::string& usertype) {
    colorcout("cyan", "Opening file explorer for user: " + username + " with type: " + usertype + "\n");
    // Here you would add the actual code to open the file explorer
}